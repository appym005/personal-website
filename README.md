# PersonalWebsite

Static personal brand site (Tailwind via CDN).

## Run locally
Any static server works.

- VS Code: install **Live Server** → right click `index.html` → **Open with Live Server**
- Python:
  - `python3 -m http.server 5173` then open `http://localhost:5173/index.html`

## Notes
- Content currently matches the approved canvas design direction.
- Next step: wire project/experience sections from normalized JSON once the layout is locked.
